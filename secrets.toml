# ============================================================
# 本地測試用 secrets.toml
# ============================================================
#
# 這是「本機測試」用的檔案，裡面已經放了一組測試密碼，
# 讓你可以馬上 streamlit run 起來試用整個付費牆流程。
#
# ⚠️ 注意：
#   - 部署到 Streamlit Community Cloud 時，
#     不要把這個檔案上傳到 GitHub（.gitignore 已經幫你排除了）
#   - 正式上線要用真的密碼時，
#     請到 Streamlit Cloud 後台的 Settings -> Secrets 裡設定，
#     不要用這組測試密碼
#
# ============================================================

ACCESS_CODES = [
    # 測試用：永久有效（不會過期），本機測試方便使用
    "TEST99",

    # 範例：只在 2026-08 這個月有效，模擬「當月授權碼」的情境
    "JIANPU-2026-08:2026-08",
]
